version: '3.8'

services:
  eduadapt-web:
    container_name: eduadapt-frontend
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"  # Maps localhost:8080 to container:80
    restart: always